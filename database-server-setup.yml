# Database
---
  - hosts: database
    become: true
    tasks:
      - name: Install mysql
        apt:
          name: mysql-server
          state: present
          update_cache: yes
      - name: Check mysql
        service:
          name: mysql
          state: started
          enabled: yes
