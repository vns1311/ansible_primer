# Load Balancer Setup Steps
---
  - hosts: loadbalancer
    become: true
    tasks:
      - name: Install nginx
        apt:
          name: nginx
          state: present
          update_cache: yes
      - name: Check if nginx is running
        service:
          name: nginx
          state: started
          enabled: yes
